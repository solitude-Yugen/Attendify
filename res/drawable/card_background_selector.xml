<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:state_pressed="true" android:drawable="@color/card_pressed" />
    <!-- Focused state -->
    <item android:state_focused="true" android:drawable="@color/card_focused" />
    <!-- Default state -->
    <item android:drawable="@color/card_default" />
</selector>